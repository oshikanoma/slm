# Core scoring (eval.py `score` and `render`) needs only the Python 3.10+ stdlib.
# The extras below are optional, grouped by which subcommand needs them.

# --- LLM-as-judge (eval.py score --judge) ---
# Requires OPENAI_API_KEY. Works with any OpenAI-compatible endpoint via --judge-base-url.
openai>=1.0

# --- Local generation (eval.py generate) ---
# Uncomment when running predictions on a HF model / your QLoRA adapter.
# transformers>=4.44
# torch>=2.2
# peft>=0.11
# accelerate>=0.33
