# Secrets — never commit
.env
.env.*
*_env
*.slm_teacher_env
*.key
secrets*

# Model artifacts (large; downloaded from Colab, regenerable)
outputs/
artifacts/
*.safetensors
*-lora/
*-lora*.zip
*.ckpt

# Prediction outputs (regenerated per run)
base_preds*.jsonl
tuned_preds*.jsonl
calib*.jsonl
# transient scoring output (keep the committed results.v*.colab.md evidence files)
/results.md
/results-*.md

# Local junk / scratch data + backups
data/junk*.jsonl
data/train.junk*.jsonl
data/*.sample.jsonl
data/synth_chunks/
data/v1_backup/
data/v2_backup/
data/tt.excluded.json
data/wikinews.excluded.json

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
